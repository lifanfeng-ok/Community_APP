<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.community">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Community">
        <activity android:name=".number_situp"></activity>
        <activity android:name=".number_squat" />
        <activity android:name=".time_situp" />
        <activity android:name=".time_squat" />
        <activity android:name=".edit_info" />
        <activity android:name=".do_regular" />
        <activity android:name=".Player" />
        <activity android:name=".My_notice" />
        <activity android:name=".My_collect" />
        <activity android:name=".index" />
        <activity android:name=".register" /> <!-- <meta-data -->
        <!-- android:name="com.google.android.actions" -->
        <!-- android:resource="@xml/actions" /> -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>